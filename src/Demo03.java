public class Demo03 {
    // 属性：变量
    //实体变量：从属于变量；如果不自行初始化，这个类型的默认值是0 0.0
    //布尔值：默认的是false
    //除了基本类型，其余的默认值都是null;
    String name;
    int age;
    static double salary = 2500;
    public static void main(String [] args){
        System.out.println(salary);
    }
}
